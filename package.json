{
  "name": "college-matchmaking",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "serve": "vite",
    "emulators": "firebase emulators:start",
    "deploy": "firebase deploy",
    "clean:shadow:win": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"Get-ChildItem -Path src -Recurse -Filter *.js | ForEach-Object { $ts=$_.FullName -replace '\\.js$', '.tsx'; $jsx=$_.FullName -replace '\\.js$', '.jsx'; if (Test-Path $ts -or Test-Path $jsx) { Remove-Item -LiteralPath $_.FullName -Force } }; Get-ChildItem -Path src -Recurse -Filter *.js.map | ForEach-Object { $tsx=$_.FullName -replace '\\.js\\.map$', '.tsx'; $jsx=$_.FullName -replace '\\.js\\.map$', '.jsx'; if (Test-Path $tsx -or Test-Path $jsx) { Remove-Item -LiteralPath $_.FullName -Force } }\"",
    "clean:shadow:nix": "bash -lc \"shopt -s globstar; for f in src/**/*.js; do base=${f%.js}; if [[ -e ${base}.tsx || -e ${base}.jsx ]]; then rm -f \\\"$f\\\"; fi; done; for m in src/**/*.js.map; do base=${m%.js.map}; if [[ -e ${base}.tsx || -e ${base}.jsx ]]; then rm -f \\\"$m\\\"; fi; done\""
  },
  "dependencies": {
    "@firebase/messaging": "^0.12.23",
    "browser-image-compression": "^2.0.2",
    "firebase": "^12.4.0",
    "framer-motion": "^12.23.25",
    "lucide-react": "^0.562.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.1",
    "react-swipeable": "^7.0.2",
    "sonner": "^1.5.0",
    "swiper": "^12.0.3"
  },
  "devDependencies": {
    "@types/react": "^18.3.24",
    "@types/react-dom": "^18.3.7",
    "@vitejs/plugin-react": "^4.3.1",
    "eslint": "^9.10.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react-hooks": "^5.1.0",
    "eslint-plugin-react-refresh": "^0.4.7",
    "postcss": "^8.4.47",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vite-plugin-pwa": "^1.0.3"
  }
}
